package ui

const (
	// UpdateErr is the error message displayed when updating a secret fails.
	UpdateErr = "Failed update secret"
	// CreateErr is the error message displayed when creating a new secret fails.
	CreateErr = "Failed create secret"
	// GetErr is the error message displayed when retrieving a secret fails.
	GetErr = "Failed get secret"
	// DeleteErr is the error message displayed when deleting a secret fails.
	DeleteErr = "Failed delete secret"
	// SyncErr is the error message displayed when synchronizing a secret fails.
	SyncErr = "Failed sync secret"
)
